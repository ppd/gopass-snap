#!/bin/bash

REALHOME=`getent passwd $UID | cut -d ':' -f 6`

ln -snf "$REALHOME/.gnupg" "$HOME/.gnupg"

mkdir -p "$SNAP_USER_COMMON/password-store"
ln -snf "$SNAP_USER_COMMON/password-store" "$HOME/.password-store"

exec "${@}"
